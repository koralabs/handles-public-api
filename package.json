{
  "name": "handles-public-api",
  "version": "1.9.0",
  "description": "A downloadable and containerized decentralized API for Handles",
  "main": "index.js",
  "repository": "https://github.com/koralabs/handles-public-api.git",
  "author": "Papa Goose <papa.goose@koralabs.io>",
  "license": "Mozilla Public License 2.0",
  "type": "module",
  "module": "esnext",
  "scripts": {
    "start": "(export NODE_ENV=development; export NETWORK=preview; export ENABLE_DATUM_ENDPOINT=true; cd dist && node express.js)",
    "start:forever": "mkdir -p forever && forever -p ./forever --minUptime 1 start express.js",
    "start:ogmios": "chmod +x ./start_local.sh && ./start_local.sh",
    "copy-dist-files": "mkdir -p dist && cp rollup.package.json dist/package.json && cp docs/swagger.yml dist/swagger.yml && cp setup_env.sh dist/setup_env.sh",
    "build:production": "yarn copy-dist-files && (cd dist && npm install && chmod +x setup_env.sh && ./setup_env.sh) && yarn run rollup -c",
    "build:docker": "yarn build:production && docker build -t koralabs/handles-api .",
    "dev:mainnet": "(export NODE_ENV=production; export NETWORK=mainnet; export ENABLE_DATUM_ENDPOINT=true; yarn start:ogmios && nodemon)",
    "dev:preprod": "(export NODE_ENV=development; export NETWORK=preprod; export ENABLE_DATUM_ENDPOINT=true; yarn start:ogmios && nodemon)",
    "dev:preview": "(export NODE_ENV=development; export NETWORK=preview; export ENABLE_DATUM_ENDPOINT=true; yarn start:ogmios && nodemon)",
    "test": "jest --silent --forceExit --detectOpenHandles --coverage",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint --ext=.js,.ts --fix --max-warnings=0 ."
  },
  "dependencies": {
    "@cardano-ogmios/client": "^6.5.0",
    "@cardano-ogmios/schema": "^6.5.0",
    "@koralabs/kora-labs-common": "5.1.1",
    "bech32": "^2.0.0",
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.5",
    "express": "^4.18.1",
    "fastq": "^1.14.0",
    "forever": "^4.0.3",
    "proper-lockfile": "^4.1.2",
    "swagger-ui-express": "^4.5.0",
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^26.0.1",
    "@rollup/plugin-json": "^6.1.0",
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@types/cors": "^2.8.12",
    "@types/express": "^4.17.14",
    "@types/jest": "^29.0.3",
    "@types/supertest": "^2.0.12",
    "@types/swagger-ui-express": "^4.1.3",
    "@types/ws": "^8.5.3",
    "@typescript-eslint/eslint-plugin": "^8.5.0",
    "@typescript-eslint/parser": "^8.5.0",
    "aws-sdk": "^2.1224.0",
    "dotenv": "^16.0.2",
    "jest": "^29.0.3",
    "nodemon": "^2.0.20",
    "prettier": "^2.7.1",
    "prettier-eslint": "^15.0.1",
    "prettier-eslint-cli": "^7.1.0",
    "rollup": "^4.21.1",
    "rollup-plugin-multi-input": "^1.5.0",
    "rollup-plugin-typescript2": "^0.36.0",
    "supertest": "^6.2.4",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.1.0",
    "tsx": "^4.17.0",
    "typescript": "^4.8.3"
  }
}
